/otherServices
/inheritance
/bankruptcy
/criminal-law
/arbitration
/cases
/intellectualRight
/realEstate
/taxes
/aboutUs
/contactsWithUs
/publications

 

  text: "Услуги",






           
        bankruptcyCitizens

bankruptcyEnterprises



            



"Банкротство граждан", "Банкротство предприятий"












      <div id='consumerRights' className='refElement'>
                ЗАЩИТА ПРАВ ПОТРЕБИТЕЛЕЙ
              </div>














                  

          { isOpen ? <li
            className={cls.menuItem}
            style={{ cursor: "pointer", color: isWhite ? "white" : "#000" }}
            onClick={handleOpen}
          >
            Услуги
          </li>
          {Services.map(({ href, text }) => {
            return (
              <li className={cls.menuItem} key={text}>
                <Link href={href}>
                  <span
                    style={{
                      color: isWhite ? "white" : "#000",
                      borderBottom:
                        pathname === href ? "1px solid #000" : "none",
                    }}
                    className={cls.menuItemText}
                  >
                    {text}
                  </span>
                </Link>
              </li>
            );
          })}
        </>
      ) : (
        <li
          className={cls.menuItem}
          style={{ cursor: "pointer", color: isWhite ? "white" : "#000" }}
          onClick={handleOpen}
        >
          Услуги
        </li>
      )}